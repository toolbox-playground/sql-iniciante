Dados usados para geração da massa de dados.

 O arquivo `.csv` contém dados dos pokemons.
 O arquivo `poke.py` é necessário para gerar os inserts. Os inserts serão exibidos no terminal, copie e cole no banco de dados.
